# PRD: FPS视频智能精彩集锦生成器

## 1. Product overview

### 1.1 Document title and version

- PRD: FPS视频智能精彩集锦生成器（FPS Video Snap）
- Version: 1.0.0
- Date: 2026年1月11日

### 1.2 Product summary

FPS视频智能精彩集锦生成器是一个自动化视频处理工具，专为FPS和MOBA类游戏设计。该工具通过AI视觉识别技术自动检测游戏视频中的击杀画面，智能截取精彩片段，并自动拼接生成带背景音乐和转场效果的高质量集锦视频。

该项目采用轻量级YOLO模型结合OpenCV的混合识别方案，充分利用NVIDIA 4070 Ti Super显卡性能，在保证识别准确率的同时实现高效处理。用户只需提供游戏录像和配置文件，工具即可自动完成从识别到输出的全流程处理。

初期版本专注于战地6（Battlefield 6）游戏的击杀识别，并提供可扩展的配置系统以支持后续添加更多游戏。

## 2. Goals

### 2.1 Business goals

- 为个人玩家提供自动化的游戏精彩集锦生成工具
- 减少手动编辑视频的时间成本，将数小时的剪辑工作缩短至几分钟
- 建立可扩展的游戏识别框架，为未来支持更多游戏类型奠定基础
- 提供可调优的识别配置系统，保存历史调试数据用于持续改进

### 2.2 User goals

- 快速从长时间游戏录像中提取所有击杀精彩片段
- 获得专业水平的视频集锦，包含转场效果和背景音乐
- 能够根据不同游戏和个人偏好调整识别参数和片段时长
- 在处理过程中可查看中间结果以验证识别准确性
- 获得详细的处理报告，包含片段数量、时长等统计信息

### 2.3 Non-goals

- 不提供图形用户界面（GUI），仅通过命令行和配置文件操作
- 不支持实时流媒体处理，仅处理本地视频文件
- 不包含视频录制功能，用户需自行使用OBS等工具录制
- 初期版本不支持战地6以外的其他游戏
- 不提供云端处理服务，所有处理均在本地完成
- 不包含视频上传或分享功能

## 3. User personas

### 3.1 Key user types

- **主要用户**：热爱FPS游戏的个人玩家，希望制作精彩集锦分享或留存

### 3.2 Basic persona details

- **游戏内容创作者**：经常录制游戏视频，希望快速生成精彩集锦用于个人欣赏或社交媒体分享。具备基本的命令行操作能力，拥有支持CUDA的NVIDIA显卡，能够根据文档配置和调试工具参数。

### 3.3 Role-based access

- **单用户模式**：所有功能对单一用户开放，无需权限管理系统

## 4. Functional requirements

### 4.1 视频帧提取与快照生成（Priority: High）

- 支持读取常见视频格式（MP4、AVI、MKV等）
- 按照可配置的间隔（默认1秒）提取视频帧
- 将提取的帧保存为临时图像文件用于识别
- 记录每一帧对应的精确时间戳

### 4.2 AI视觉识别系统（Priority: High）

- 集成YOLOv8-nano模型用于游戏UI元素检测
- 使用OpenCV进行辅助特征匹配和验证
- 支持通过配置文件定义不同游戏的击杀特征（UI位置、颜色特征、文字识别等）
- 记录识别到击杀事件的时间戳到日志文件
- 提供置信度评分，过滤误检结果

### 4.3 精彩片段提取（Priority: High）

- 根据识别到的时间戳，提取击杀前N秒和击杀后M秒的视频片段（默认前5秒后2秒）
- 片段时长可在游戏配置文件中自定义
- 支持片段重叠时的智能合并，避免重复内容
- 为每个片段生成唯一标识符和元数据
- 可选保留中间视频片段用于调试

### 4.4 连续击杀检测与处理（Priority: High）

- 检测连续击杀事件：当下一次击杀距离前一次击杀在指定时间内（默认10秒，可配置）
- 将连续击杀识别为单个精彩片段，而不是多个独立片段
- 连续击杀片段时间范围：从第一次击杀前N秒开始，到最后一次击杀后M秒结束
- 在片段元数据中标记连续击杀数量（双杀、三连杀等）
- 支持配置连续击杀判定时间阈值
- 在报告中特别标注连续击杀片段

### 4.5 视频拼接与转场效果（Priority: High）

- 使用FFmpeg将多个片段按时间顺序拼接
- 在片段之间随机应用多种转场效果（淡入淡出、闪白、滑动、缩放等）
- 保持输入输出分辨率一致（1080p 60fps）
- 优化编码参数确保视频质量

### 4.6 背景音乐集成（Priority: Medium）

- 支持用户提供自定义背景音乐文件（MP3、WAV等）
- 自动调整音乐时长以匹配最终视频长度（循环或淡出）
- 混合原视频音频和背景音乐，可配置音量比例
- 保留游戏内音效的同时添加背景音乐层

### 4.7 配置管理系统（Priority: High）

- 使用YAML或JSON格式的配置文件
- 支持全局配置（输出路径、帧提取间隔、调试模式等）
- 支持游戏特定配置（击杀特征、片段时长、识别参数等）
- 自动保存每次运行的配置快照和识别结果作为历史记录
- 提供配置模板和详细注释

### 4.8 输出管理与清理（Priority: Medium）

- 最终视频输出到指定的output目录
- 自动清理临时截图文件
- 保留时间戳日志文件（JSON或CSV格式）
- 可选保留中间视频片段（通过配置开关）
- 生成Markdown格式的处理报告

### 4.9 报告生成（Priority: Medium）

- 生成详细的处理报告（Markdown格式）
- 包含信息：处理时间、识别到的击杀数量、每个片段的时间点、最终视频时长、识别置信度统计等
- 报告文件与输出视频保存在同一目录
- 支持追加模式记录多次处理结果

## 5. User experience

### 5.1 Entry points & first-time user flow

- 用户克隆或下载项目代码到本地
- 运行安装脚本配置Python环境和依赖（PyTorch、OpenCV、FFmpeg等）
- 复制配置模板并根据需要修改参数
- 准备游戏视频文件和背景音乐文件
- 通过命令行执行主程序，传入视频路径和配置文件路径

### 5.2 Core experience

- **视频分析阶段**：程序显示进度条，实时展示当前处理的帧数和识别状态
  - 确保用户了解处理进度，避免因长时间等待产生焦虑

- **片段提取阶段**：显示识别到的击杀数量和时间点列表
  - 让用户快速验证识别准确性，如果结果不理想可中止并调整配置

- **视频合成阶段**：展示拼接进度和预计完成时间
  - 提供清晰的状态反馈，特别是在处理大量片段时

- **完成阶段**：输出最终视频路径和处理报告摘要
  - 用户可立即预览生成的视频并查看详细统计

### 5.3 Advanced features & edge cases

- 如果未检测到任何击杀，提示用户检查配置或视频内容
- 如果识别到的击杀数量异常多，警告可能存在误检
- 支持断点续传，如果处理中断可从上次位置继续
- 提供详细日志文件用于调试识别问题
- 支持批量处理多个视频文件

### 5.4 UI/UX highlights

- 清晰的命令行输出，使用颜色区分不同信息级别（成功/警告/错误）
- 实时进度条显示当前处理状态
- 简洁的配置文件结构，带详细注释和示例
- 友好的错误提示，包含可能的解决方案
- 完整的README文档，包含安装、配置、使用示例

## 6. Narrative

想象你刚完成一场3小时的战地6游戏马拉松，录制了一段巨大的视频文件。你知道其中有很多精彩击杀瞬间，但手动查找和剪辑需要几个小时。

现在，你只需运行FPS Video Snap，程序会自动分析整个视频，识别出每一次击杀，提取前后几秒的精彩画面，添加炫酷的转场效果和你最爱的背景音乐，最终生成一个3分钟的高质量集锦视频。

整个过程完全自动化，你只需等待几分钟，就能获得专业水平的视频作品。如果想调整识别灵敏度或片段长度，只需修改配置文件即可。所有的识别数据都会保存下来，方便你持续优化配置，让识别越来越准确。

## 7. Success metrics

### 7.1 User-centric metrics

- 击杀识别准确率达到90%以上（正确识别的击杀数 / 实际击杀数）
- 误检率低于5%（错误识别的片段数 / 总识别片段数）
- 平均处理速度：1小时原视频在10分钟内完成处理
- 用户首次成功运行时间少于30分钟（包含环境配置）

### 7.2 Business metrics

- 节省视频编辑时间：相比手动剪辑减少95%的时间投入
- 配置调优成功率：用户能在3次尝试内达到满意的识别效果

### 7.3 Technical metrics

- GPU利用率达到60%以上，充分发挥4070 Ti Super性能
- 内存占用控制在8GB以内
- 临时文件总大小不超过原视频的50%
- 最终输出视频质量损失小于5%（VMAF评分）
- 系统稳定性：连续处理5个以上视频不出现崩溃或内存泄漏

## 8. Technical considerations

### 8.1 Integration points

- **FFmpeg**：用于视频解码、帧提取、片段切割、转场效果、视频拼接、音频混合
- **PyTorch + YOLOv8**：轻量级目标检测模型，识别游戏UI元素（击杀提示、图标等）
- **OpenCV**：辅助图像处理、特征匹配、模板匹配、颜色检测
- **CUDA/cuDNN**：GPU加速推理，提升识别速度
- **Python生态**：使用NumPy、Pandas处理数据，使用YAML/JSON解析配置

### 8.2 Data storage & privacy

- 所有处理完全本地化，不涉及数据上传或外部服务
- 临时文件存储在可配置的临时目录
- 时间戳日志以JSON格式保存，便于程序读取和人工查看
- 配置历史记录按时间戳命名，保存在独立目录
- 最终输出目录结构清晰，易于用户管理和归档

### 8.3 Scalability & performance

- 使用多线程或异步IO处理帧提取，提高磁盘读写效率
- 批量推理视频帧，充分利用GPU并行能力
- 支持可配置的帧提取间隔，在准确性和速度间平衡
- FFmpeg使用硬件加速（NVENC）进行视频编码
- 实现智能缓存机制，避免重复处理相同视频片段

### 8.4 Potential challenges

- **识别准确率**：不同光照、UI设置、HUD位置可能影响识别效果
  - 解决方案：提供多种识别策略组合，支持用户自定义ROI区域
  
- **性能优化**：长视频处理可能耗时较长
  - 解决方案：实现增量处理和断点续传，优化GPU利用率
  
- **视频格式兼容性**：各种录制软件产生的视频格式不同
  - 解决方案：依赖FFmpeg的广泛格式支持，提供格式检测和转换功能
  
- **片段边界处理**：击杀事件密集时可能产生重叠片段
  - 解决方案：实现智能合并算法和连续击杀检测，避免重复内容并突出连杀片段
  
- **配置复杂性**：首次配置可能对新手有门槛
  - 解决方案：提供预设配置模板和详细文档，包含常见场景的推荐设置

## 9. Milestones & sequencing

### 9.1 Project estimate

- **Size**: 中型项目
- **Time estimate**: 4-6周开发周期

### 9.2 Team size & composition

- **Team size**: 1人（个人项目）
- **Roles involved**: 全栈开发（Python后端、视频处理、AI模型集成）

### 9.3 Suggested phases

- **Phase 1: 基础框架与视频处理**（1周）
  - 搭建项目结构和配置系统
  - 实现FFmpeg集成：视频读取、帧提取、基本拼接
  - 开发命令行接口和日志系统
  - 交付：能够按固定时间戳提取和拼接视频片段的基础版本

- **Phase 2: AI识别系统**（1.5周）
  - 集成YOLOv8模型和训练环境
  - 实现OpenCV辅助识别模块
  - 开发战地6击杀特征识别逻辑
  - 实现时间戳记录和置信度评分
  - 交付：能够自动识别击杀事件并记录时间戳的版本

- **Phase 3: 智能片段提取与拼接**（1周）
  - 实现基于时间戳的智能片段提取
  - 开发片段重叠检测与合并算法
  - 集成多种转场效果
  - 实现中间文件管理和清理
  - 交付：能够生成带转场效果的集锦视频

- **Phase 4: 音频处理与优化**（0.5周）
  - 实现背景音乐集成和音频混合
  - 优化视频编码参数和质量
  - 实现GPU硬件加速
  - 性能调优和内存优化
  - 交付：完整功能的1.0版本

- **Phase 5: 报告系统与文档**（1周）
  - 开发Markdown报告生成模块
  - 实现配置历史记录功能
  - 编写完整的用户文档和API文档
  - 准备配置模板和示例
  - 进行端到端测试和bug修复
  - 交付：生产就绪的完整版本

## 10. User stories

### 10.1. 视频输入与格式验证

- **ID**: GH-001
- **Description**: 作为用户，我希望能够指定输入视频文件路径，系统能自动验证文件格式和完整性，以便确保后续处理能够正常进行。
- **Acceptance criteria**:
  - 支持通过命令行参数传入视频文件路径
  - 验证文件是否存在且可读
  - 检测视频格式是否为支持的类型（MP4、AVI、MKV等）
  - 提取并显示视频基本信息（分辨率、帧率、时长、编码格式）
  - 如果文件无效或格式不支持，显示清晰的错误提示
  - 支持相对路径和绝对路径

### 10.2. 配置文件加载与验证

- **ID**: GH-002
- **Description**: 作为用户，我希望能够通过配置文件自定义各项处理参数，系统能自动验证配置有效性，以便灵活控制识别和输出行为。
- **Acceptance criteria**:
  - 支持YAML格式的配置文件
  - 配置文件包含：全局设置（输出路径、调试模式等）、游戏特定设置（击杀特征、片段时长等）
  - 如未指定配置文件，加载默认配置
  - 验证所有必需字段是否存在
  - 验证数值参数的合理范围（如帧提取间隔>0）
  - 配置错误时提供详细的错误位置和修正建议
  - 启动时打印当前使用的配置摘要

### 10.3. 视频帧提取与时间戳记录

- **ID**: GH-003
- **Description**: 作为用户，我希望系统能够按固定间隔提取视频帧并保存为图像，同时记录每帧的精确时间戳，以便后续进行击杀识别。
- **Acceptance criteria**:
  - 使用FFmpeg按配置的间隔（默认1秒）提取帧
  - 将帧保存为JPEG格式到临时目录
  - 每个图像文件名包含对应的时间戳信息
  - 显示提取进度（当前帧数/总帧数）
  - 提取完成后输出总帧数和耗时统计
  - 临时目录结构清晰，便于调试查看
  - 支持可配置的帧提取间隔（0.5秒、1秒、2秒等）

### 10.4. AI模型加载与初始化

- **ID**: GH-004
- **Description**: 作为用户，我希望系统能够自动加载YOLOv8模型并初始化GPU环境，以便高效进行击杀识别。
- **Acceptance criteria**:
  - 首次运行时自动下载YOLOv8-nano模型权重
  - 检测CUDA是否可用，优先使用GPU推理
  - 如GPU不可用，自动降级到CPU推理并提示用户
  - 显示模型加载状态和使用的设备（CPU/GPU）
  - 预热模型（首次推理），确保后续推理速度稳定
  - 处理模型加载失败的异常情况
  - 输出GPU显存占用信息

### 10.5. 击杀特征识别与时间戳记录

- **ID**: GH-005
- **Description**: 作为用户，我希望系统能够自动识别每一帧中的击杀提示UI元素，记录击杀发生的时间戳，以便后续提取精彩片段。
- **Acceptance criteria**:
  - 对每一帧图像运行YOLOv8检测
  - 根据游戏配置文件中定义的击杀特征进行匹配（UI位置、图标、文字等）
  - 使用OpenCV进行二次验证（颜色特征、模板匹配等）
  - 每次识别到击杀时记录精确时间戳和置信度评分
  - 过滤低置信度的识别结果（阈值可配置）
  - 实时显示识别进度和当前检测到的击杀数量
  - 将所有识别结果保存到JSON格式的时间戳文件
  - 处理完成后输出识别统计（总击杀数、平均置信度等）

### 10.6. 自定义击杀特征配置

- **ID**: GH-006
- **Description**: 作为用户，我希望能够通过配置文件自定义战地6的击杀识别特征，包括UI位置、颜色、图标等，以便适应不同的游戏设置和显示环境。
- **Acceptance criteria**:
  - 配置文件支持定义感兴趣区域（ROI）坐标
  - 支持定义击杀UI的颜色特征（HSV范围）
  - 支持上传自定义的击杀图标模板用于模板匹配
  - 支持配置置信度阈值和识别策略组合
  - 提供战地6的默认配置模板作为参考
  - 配置文件包含详细注释说明每个参数的作用
  - 修改配置后无需重新编译，重新运行即可生效

### 10.7. 智能片段提取与重叠处理

- **ID**: GH-007
- **Description**: 作为用户，我希望系统能够根据识别到的时间戳提取击杀前后的视频片段，并智能处理重叠片段，避免重复内容。
- **Acceptance criteria**:
  - 根据配置的前后时长（默认前5秒后2秒）计算每个片段的起止时间
  - 检测片段之间的时间重叠
  - 自动合并重叠或相邻的片段，生成更长的连续片段
  - 使用FFmpeg精确切割视频片段，保持原始质量
  - 为每个片段生成唯一文件名（包含序号和时间戳）
  - 保存片段元数据（起止时间、原始时间戳、是否合并、击杀数量等）
  - 如配置启用调试模式，保留中间视频片段
  - 显示片段提取进度和已完成数量

### 10.8. 多样化转场效果应用

- **ID**: GH-008
- **Description**: 作为用户，我希望系统能够在拼接视频片段时自动添加多种转场效果，让最终视频更具观赏性。
- **Acceptance criteria**:
  - 支持至少5种转场效果（淡入淡出、闪白、滑动、缩放、旋转）
  - 在片段之间随机选择转场效果
  - 转场时长可配置（默认0.5秒）
  - 确保转场效果不影响视频流畅度
  - 转场效果使用FFmpeg滤镜实现，保持高性能
  - 支持配置禁用转场效果（直接拼接）
  - 在报告中记录使用的转场效果类型

### 10.9. 背景音乐集成与音频混合

- **ID**: GH-009
- **Description**: 作为用户，我希望能够为最终视频添加自定义背景音乐，系统能自动调整音乐长度并与游戏音效合理混合。
- **Acceptance criteria**:
  - 支持通过配置指定背景音乐文件路径（MP3、WAV等格式）
  - 如音乐短于视频，自动循环播放音乐
  - 如音乐长于视频，截取前段或淡出结束
  - 可配置原始音频和背景音乐的音量比例（默认游戏音50%、音乐50%）
  - 使用FFmpeg音频滤镜进行高质量混合
  - 支持不添加背景音乐的选项（保留原始音频）
  - 验证音频文件有效性，格式不支持时给出提示

### 10.10. 最终视频拼接与编码

- **ID**: GH-010
- **Description**: 作为用户，我希望系统能够将所有片段拼接成一个完整视频，保持1080p 60fps的高质量输出。
- **Acceptance criteria**:
  - 按时间顺序拼接所有提取的片段
  - 应用转场效果和音频混合
  - 输出分辨率保持1080p，帧率保持60fps
  - 使用H.264编码，CRF值可配置（默认18，高质量）
  - 如GPU支持，使用NVENC硬件加速编码
  - 显示拼接和编码进度
  - 最终视频保存到配置的输出目录
  - 文件名包含时间戳和视频统计（如`battlefield6_highlights_20260111_8kills_180s.mp4`）

### 10.11. 处理报告生成

- **ID**: GH-011
- **Description**: 作为用户，我希望系统能够生成详细的Markdown格式处理报告，包含所有识别和处理的统计信息，便于查看和归档。
- **Acceptance criteria**:
  - 报告文件名与输出视频对应，保存在同一目录
  - 报告包含：处理时间、输入视频信息、识别到的击杀数量、每个片段的详细信息（时间点、时长、置信度）
  - 包含：最终视频时长、总片段数、识别准确率估计、处理耗时统计
  - 报告结构清晰，使用Markdown表格展示片段列表
  - 包含使用的配置参数摘要
  - 如有识别失败或警告，在报告中标注
  - 报告文件编码为UTF-8，确保中文正常显示

### 10.12. 临时文件清理与中间结果保留

- **ID**: GH-012
- **Description**: 作为用户，我希望系统能够自动清理处理过程中的临时文件，但保留重要的中间结果用于调试和调优。
- **Acceptance criteria**:
  - 处理完成后自动删除所有临时截图文件
  - 保留时间戳JSON文件到输出目录
  - 如配置启用调试模式（`keep_intermediate_clips: true`），保留所有中间视频片段到单独子目录
  - 如配置禁用调试模式，自动删除中间视频片段
  - 清理操作在最终视频生成后进行
  - 清理失败不影响主流程，仅记录警告日志
  - 显示清理统计（删除的文件数、释放的磁盘空间）

### 10.13. 配置历史记录与版本管理

- **ID**: GH-013
- **Description**: 作为用户，我希望系统能够自动保存每次运行的配置快照和识别结果，便于对比不同参数的效果，持续优化识别准确率。
- **Acceptance criteria**:
  - 每次运行时将当前配置保存到`history`目录
  - 配置快照文件名包含时间戳（如`config_20260111_143025.yaml`）
  - 同时保存对应的识别结果和统计信息（JSON格式）
  - 历史记录包含：配置参数、识别到的击杀数、置信度分布、处理耗时等
  - 提供简单的脚本或工具对比不同版本的配置效果
  - 历史记录目录大小可控，提供清理旧记录的选项
  - 历史记录不影响主流程性能

### 10.14. 错误处理与日志记录

- **ID**: GH-014
- **Description**: 作为用户，我希望系统能够妥善处理各种异常情况，提供清晰的错误提示和详细的日志文件，便于问题排查。
- **Acceptance criteria**:
  - 捕获所有可能的异常（文件不存在、格式错误、内存不足、GPU错误等）
  - 错误提示清晰具体，包含可能的原因和解决建议
  - 记录详细的日志文件，包含时间戳、日志级别、模块名称、详细信息
  - 日志输出到文件和控制台，控制台输出使用颜色区分级别
  - 支持配置日志级别（DEBUG、INFO、WARNING、ERROR）
  - 关键步骤（模型加载、识别开始/结束、片段提取等）记录INFO级别日志
  - 日志文件保存到输出目录，文件名包含时间戳
  - 程序崩溃时保存完整的堆栈跟踪信息

### 10.15. 命令行接口与参数解析

- **ID**: GH-015
- **Description**: 作为用户，我希望能够通过简洁的命令行接口运行程序，支持必要的参数和选项，便于集成到自动化脚本中。
- **Acceptance criteria**:
  - 基本用法：`python main.py --video <path> --config <path>`
  - 支持必需参数：`--video`（输入视频路径）
  - 支持可选参数：`--config`（配置文件，默认`config.yaml`）、`--output`（输出目录）、`--music`（背景音乐）、`--debug`（调试模式）
  - 提供`--help`显示完整的参数说明和使用示例
  - 提供`--version`显示程序版本信息
  - 参数验证失败时显示清晰的错误和使用提示
  - 支持通过环境变量配置某些参数
  - 所有路径参数支持相对路径和绝对路径，支持路径中包含空格

### 10.16. 进度显示与状态反馈

- **ID**: GH-016
- **Description**: 作为用户，我希望在处理过程中能够实时看到进度和状态信息，了解当前处理阶段和预计完成时间。
- **Acceptance criteria**:
  - 使用进度条显示各阶段完成百分比（帧提取、识别、片段提取、视频拼接）
  - 显示当前处理速度（帧/秒）和预计剩余时间
  - 每个主要阶段开始时显示明确的状态信息
  - 实时更新识别到的击杀数量
  - 使用不同颜色区分不同类型的信息（进度-蓝色、成功-绿色、警告-黄色、错误-红色）
  - 在终端标题栏显示整体进度百分比（如支持）
  - 完成时显示总耗时和各阶段耗时分解

### 10.17. 批量处理支持

- **ID**: GH-017
- **Description**: 作为用户，我希望能够一次处理多个视频文件，系统自动按顺序处理并为每个视频生成独立的输出。
- **Acceptance criteria**:
  - 支持通过通配符或目录指定多个输入视频
  - 按顺序处理每个视频，互不影响
  - 每个视频使用相同的配置和参数
  - 为每个视频生成独立的输出目录或使用区分性文件名
  - 显示批处理进度（当前第X个/共Y个视频）
  - 某个视频处理失败不影响后续视频处理
  - 批处理完成后生成汇总报告，列出所有视频的处理结果
  - 支持配置并行处理多个视频（如资源允许）

### 10.18. 连续击杀检测与特殊处理

- **ID**: GH-018
- **Description**: 作为用户，我希望系统能够自动识别连续击杀事件，将其作为单个完整精彩片段处理，以突出连杀的高光时刻。
- **Acceptance criteria**:
  - 分析所有击杀时间戳，检测连续击杀模式
  - 连续击杀判定：当下一次击杀距离前一次击杀在配置的时间阈值内（默认10秒）
  - 将连续击杀合并为单个片段，时间范围从第一次击杀前N秒到最后一次击杀后M秒
  - 在片段元数据中标记连续击杀数量（2杀、3杀、4杀等）
  - 在片段文件名中体现连续击杀信息（如`clip_001_triple_kill.mp4`）
  - 连续击杀时间阈值可在配置文件中自定义
  - 在处理报告中单独统计连续击杀片段（双杀X次、三连杀Y次等）
  - 在日志中清晰显示检测到的连续击杀信息
  - 支持配置最小连杀数量（如只保留3杀及以上的连杀）

---

PRD文档创建完成！该文档涵盖了项目的所有关键方面，包括17个详细的用户故事，每个都具有可测试的接受标准。

你是否希望我现在根据这个PRD创建GitHub issues？如果需要，我可以为每个用户故事创建对应的issue。
