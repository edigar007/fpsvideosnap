# FPS Video Snap 第 7 轮代码评测报告

评测日期：2026-06-08  
评测对象：当前 `main` 分支，最新提交 `782090c`  
评测轮次：第 7 轮  
输出人：GPT

## 总体结论

第 6 轮维护整改后，仓库已经达到稳定维护状态。当前全量 `ruff`、`pytest`、`compileall` 均通过，测试结果为 `309 passed, 1 skipped`。上一轮只做了小修：typed config bool 解析不再误读 `"false"`/`"0"`，多视频 merge 在 join/audio/report 步骤之间支持轻量 cancel，验证记录和已知限制文档也已经落地。

本轮没有发现新的阻断级问题，也不建议继续开启新的 review/remediation 循环。继续迭代代码架构的收益已经很低，主要风险反而会变成“为了提升分数而引入更多抽象、更多 mock 测试、更多文档轮次”。当前最重要的缺口不是代码结构，而是缺少真实 FPS 视频样本验证。

建议到此停止连续整改。后续工作应从“工程评分优化”切换为“真实样本验证、发布准备、实际问题驱动修复”。

## 评分

| 维度 | 分数 | 说明 |
|---|---:|---|
| 总体工程质量 | 8.8 / 10 | 自动化门禁稳定，前几轮关键风险已闭环；继续追分不划算。 |
| 架构清晰度 | 8.6 / 10 | Pipeline、Config Assistant、VideoJoiner 的主要边界已成形；剩余复杂度符合业务本身。 |
| 代码质量 | 8.7 / 10 | lint 全绿，配置 bool 归一化修正，cancel 语义更完整。 |
| 可测试性 | 9.0 / 10 | 309 个测试通过，关键回归点有覆盖；无需为了测试数继续堆 mock。 |
| 可运维性 | 8.5 / 10 | 已知限制和验证记录落地；真实样本验证仍缺失。 |
| 配置驱动程度 | 8.8 / 10 | typed view 覆盖高频 detection section，校验和兼容读取边界更清楚。 |

## 当前验证结果

```powershell
.venv\Scripts\python.exe -m ruff check . --statistics
.venv\Scripts\python.exe -m pytest tests/ -q
.venv\Scripts\python.exe -m compileall -q src main.py scripts
```

结果：

- `ruff` 通过。
- `pytest`：`309 passed, 1 skipped in 7.20s`。
- `compileall` 通过。

## 规模与热点

- `src`：96 个 Python 文件，约 8661 行。
- `tests`：62 个 Python 文件，约 7227 行。
- `except Exception` / bare `except` 相关匹配约 58 处。
- 最大源码文件：
  - `src/pipeline/pipeline.py`：514 行。
  - `src/ai/ocr_detector.py`：351 行。
  - `src/tools/dashboard/task_manager.py`：307 行。
  - `src/tools/config_assistant/services/config_test_service.py`：304 行。
  - `src/tools/dashboard/worker.py`：287 行。
  - `src/video/frame_extractor.py`：268 行。
  - `src/ai/opencv_matcher.py`：249 行。
  - `src/ai/kill_detector.py`：245 行。
  - `src/video/video_joiner.py`：239 行。
  - `src/video/ffmpeg_command.py`：204 行。
- 最大测试文件：
  - `tests/test_kill_detector_rules_mode.py`：332 行。
  - `tests/test_pipeline_incremental_resume.py`：311 行。
  - `tests/test_config.py`：311 行。
  - `tests/test_ai_template_regressions.py`：300 行。
  - `tests/test_dashboard_task_manager.py`：300 行。
  - `tests/test_config_assistant_api.py`：286 行。
  - `tests/test_pipeline.py`：269 行。

## 是否还有必要继续迭代

结论：没有必要继续以“review 后 remediation”的形式迭代代码。

理由：

1. 自动化门禁长期稳定，全量测试已覆盖核心 pipeline、配置、Dashboard、Config Assistant、FFmpeg 命令构造和多视频 merge cancel。
2. 第 6 轮已经把上轮建议的小修完成，没有留下需要马上处理的代码级 P1/P2 缺陷。
3. 主要未闭环项是 `docs/VALIDATION_2026-06-08_gpt.md` 中明确记录的真实样本缺失，这不是靠继续拆代码能解决的问题。
4. 当前最大文件仍是 `pipeline.py`，但它承担 orchestration 职责，继续为行数拆分会带来低收益复杂度。
5. `VideoJoiner` 和 `ffmpeg_command.py` 已经达到足够清晰的边界，继续拆 probe/executor/normalizer 会让 FFmpeg 调试路径更分散。

建议停止连续 review/remediation，直到出现以下触发条件：

1. 提供真实视频样本并发现端到端失败或准确率问题。
2. Dashboard cancel、FFmpeg/NVENC、PaddleOCR、CUDA 在真实环境中出现可复现问题。
3. 新增游戏、新增检测信号、新增编码 fallback 或新增 UI 流程。
4. 发布前验收发现文档、安装或运行路径缺口。

## 主要发现

### P1：暂无阻断级问题

当前全量门禁通过，未发现会阻断主流程、测试或发布前准备的代码级缺陷。

### P2-1：发布前真实样本验证仍未完成

位置：

- `docs/VALIDATION_2026-06-08_gpt.md`
- `docs/KNOWN_LIMITATIONS.md`

问题：

仓库当前没有可用 `.mp4`、`.mkv`、`.avi`、`.mov` 样本。第 6 轮已经正确记录“缺少真实样本，待补”，没有伪造端到端结果。这是目前最重要的真实发布风险。

影响：

- 不能确认 Battlefield 6 真实录像上的 kill 检测准确率。
- 不能确认 FFmpeg/NVENC 在真实片段拼接、音频混合和转场上的表现。
- 不能确认 OCR、模板、颜色阈值在真实分辨率和 UI 缩放下是否稳定。

建议：

不要开新一轮架构整改。准备 1 个短样本和 1 个长样本，按 `docs/VALIDATION_2026-06-08_gpt.md` 执行手工验证并记录结果。只有样本验证暴露具体问题时，再按问题修复。

### P3-1：merge cancel 后可能留下中间文件，当前属于可接受已知边界

位置：

- `src/pipeline/multi_video.py:61`
- `src/pipeline/multi_video.py:64`
- `src/pipeline/multi_video.py:69`
- `src/pipeline/multi_video.py:75`

问题：

多视频 merge 在 join 后或 audio 后取消时会直接返回 cancelled 结果。如果 FFmpeg join 已经生成 `combined_temp_*`，当前函数不会在取消路径中统一清理该中间文件。

判断：

这是维护级问题，不建议马上为了它开启新整改。取消语义优先保证“不要继续后续步骤”；临时文件清理可以在真实 Dashboard 使用中观察。如果用户频繁取消长 merge 并产生明显磁盘垃圾，再补一个小型 cleanup helper。

### P3-2：测试体系已经足够，不建议继续为覆盖率拆分或堆 mock

位置：

- `tests/test_dashboard_task_manager.py`
- `tests/test_multi_video.py`
- `tests/test_ffmpeg_command_builder.py`

现状：

新增测试覆盖了 bool 解析、多视频 merge 步骤间 cancel、Dashboard worker cancelled 结果处理。当前测试数量和覆盖面已经足够支撑维护。

建议：

不要为了把单个测试文件从 300 行降到更低而机械拆分。后续测试应由真实 bug、真实样本失败或新功能驱动。

## 积极变化

1. `DetectionConfigView._as_bool()` 已修正字符串 bool 解析，避免 draft config 误读。
2. `merge_clips_to_highlight()` 支持可选 `cancel_event`，能在 join/audio/report 步骤间停止。
3. Dashboard worker 能识别 merge 内部 cancelled 结果。
4. `tests/test_multi_video.py` 覆盖了 merge cancel 的三个关键时机。
5. `docs/VALIDATION_2026-06-08_gpt.md` 没有伪造验证结果，明确记录真实样本缺失。
6. `docs/KNOWN_LIMITATIONS.md` 把环境、Dashboard cancel、Config Assistant、OCR/GPU、检测准确率边界写清楚。

## 建议后续策略

### 停止连续 remediation

本轮不建议生成新的代码整改计划。若流程上必须生成计划，也应是“发布验证计划”，而不是“架构整改计划”。

建议下一步：

1. 准备真实视频样本，补全 `docs/VALIDATION_2026-06-08_gpt.md`。
2. 跑 CLI 端到端和 Dashboard cancel 手工验证。
3. 根据真实结果决定是否修检测配置、FFmpeg 参数或文档。
4. 保持当前架构，不再为了评分继续拆分。

## 最终评价

当前仓库已经达到“工程上可以停止连续整改，转入发布验证”的状态。继续 review 可以继续找到小瑕疵，但很难证明这些小瑕疵值得引入新一轮代码改动。

最务实的判断是：停止追分，补真实样本验证。只有真实使用暴露问题时，再做有明确输入和验收标准的修复。
